import React from 'react';

const HoverInfo = ({ render }) => {
    // Students to implement hover functionality using render props
    return (
        <div>
            {/* Content with render props goes here */}
        </div>
    );
};

export default HoverInfo;
