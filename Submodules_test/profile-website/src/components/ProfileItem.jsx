import React from 'react';

const ProfileItem = ({ profile }) => {
    // Students to implement the display of profile data
    return (
        <div>
            {/* Display profile name and occupation */}
        </div>
    );
};

export default ProfileItem;
